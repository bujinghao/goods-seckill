<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seckill.mapper.OperationLogMapper">

    <resultMap id="BaseResultMap" type="com.seckill.entity.OperationLog">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="operation" property="operation" jdbcType="VARCHAR"/>
        <result column="method" property="method" jdbcType="VARCHAR"/>
        <result column="params" property="params" jdbcType="VARCHAR"/>
        <result column="result" property="result" jdbcType="VARCHAR"/>
        <result column="ip" property="ip" jdbcType="VARCHAR"/>
        <result column="location" property="location" jdbcType="VARCHAR"/>
        <result column="time_taken" property="timeTaken" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="error_msg" property="errorMsg" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 插入操作日志 -->
    <insert id="insert" parameterType="com.seckill.entity.OperationLog">
        INSERT INTO operation_log (
            user_id, username, operation, method, params, result,
            ip, location, time_taken, status, error_msg
        ) VALUES (
            #{userId}, #{username}, #{operation}, #{method}, #{params}, #{result},
            #{ip}, #{location}, #{timeTaken}, #{status}, #{errorMsg}
        )
    </insert>

    <!-- 根据用户ID查询日志 -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM operation_log
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <!-- 根据操作类型查询日志 -->
    <select id="selectByOperation" resultMap="BaseResultMap">
        SELECT * FROM operation_log
        WHERE operation = #{operation}
        ORDER BY create_time DESC
    </select>

    <!-- 根据时间范围查询日志 -->
    <select id="selectByTimeRange" resultMap="BaseResultMap">
        SELECT * FROM operation_log
        WHERE create_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY create_time DESC
    </select>

    <!-- 分页查询日志 -->
    <select id="selectByPage" resultMap="BaseResultMap">
        SELECT * FROM operation_log
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

</mapper>