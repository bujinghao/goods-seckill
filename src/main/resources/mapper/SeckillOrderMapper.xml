<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.mapper.SeckillOrderMapper">

    <resultMap id="BaseResultMap" type="com.seckill.entity.SeckillOrder">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="goods_id" property="goodsId"/>
        <result column="goods_name" property="goodsName"/>
        <result column="seckill_price" property="seckillPrice"/>
        <result column="count" property="count"/>
        <result column="total_price" property="totalPrice"/>
        <result column="status" property="status"/>
        <result column="pay_time" property="payTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_no, user_id, goods_id, goods_name, seckill_price, 
        count, total_price, status, pay_time, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO seckill_order (
            order_no, user_id, goods_id, goods_name, seckill_price, 
            count, total_price, status
        ) VALUES (
            #{orderNo}, #{userId}, #{goodsId}, #{goodsName}, #{seckillPrice},
            #{count}, #{totalPrice}, #{status}
        )
    </insert>

    <select id="selectByOrderNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order
        WHERE order_no = #{orderNo}
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM seckill_order
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="countByUserAndGoods" resultType="int">
        SELECT COUNT(1)
        FROM seckill_order
        WHERE user_id = #{userId}
          AND goods_id = #{goodsId}
          AND status IN (0, 1)
    </select>

    <update id="updateStatus">
        UPDATE seckill_order
        SET status = #{status}
        WHERE id = #{id}
    </update>

</mapper>
